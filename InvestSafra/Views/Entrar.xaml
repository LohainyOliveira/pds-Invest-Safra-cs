<Window x:Class="InvestSafra.Views.Entrar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InvestSafra.Views"
        mc:Ignorable="d"
          xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="Entrar" Height="450" Width="800">
    <Window.Background>
        <ImageBrush ImageSource="/Imagens/ImgFundoDesfocado.png"/>
    </Window.Background>
    <Grid>

        <Rectangle Height="280" VerticalAlignment="Top">
            <Rectangle.Fill>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FF2281D1"/>
                    <GradientStop Color="#FF34268A" Offset="1"/>
                    <GradientStop Color="#FF33288B" Offset="0.546"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <Border Width="280" Height="240" VerticalAlignment="Bottom" Margin="0,80"  Background="white">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="0" RenderingBias="Quality" ShadowDepth="1" Color="#FFBBBBBB"/>
            </Border.Effect>
        </Border>
        <Grid VerticalAlignment="Bottom" Margin="35,80" Height="240">
            <StackPanel VerticalAlignment="top" HorizontalAlignment="Center">
                <StackPanel>
                    <Label Content="Login" Foreground="Black" FontSize="25" HorizontalAlignment="Center"/>
                    <Separator></Separator>
                    <TextBox materialDesign:HintAssist.Hint="Usuário"  Style="{StaticResource MaterialDesignFloatingHintTextBox}" x:Name="txtUsuario" Background="#0F040302" Foreground="white" FontSize="18"></TextBox>
                    <PasswordBox  materialDesign:HintAssist.Hint="Senha"
                     x:Name="psSenha" Padding="16" Background="#0F040302"
                    FontFamily="13" FontWeight="Bold" Cursor="Arrow"/>

                    <Button x:Name="btLogar" Content="Logar" Background="Red" Foreground="black" FontSize="18" Margin="60 10" Cursor="Hand"/>
                </StackPanel>
            </StackPanel>
        </Grid>
        <Button Width="150" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,65" Content="LOGIN"/>
        <TextBlock Text="ESQUECEU A SENHA?" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="30" Foreground="Gray" Cursor="Hand"/>

    </Grid>


</Window>
