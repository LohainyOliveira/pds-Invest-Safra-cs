<Window x:Class="InvestSafra.Views.ListaArea"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Height="720" Width="1080" WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent">

    <Border Background="White" CornerRadius="30" MouseDown="Border_MouseDown" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="100"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
            </Grid>

            

            <Border Background="#EFA82E" CornerRadius="0 50 0 30" Margin="0,117,0,0">
                <DockPanel>
                    <Border Width="80" Height="80" Padding="3" Background="white" CornerRadius="100"
                            DockPanel.Dock="Top" HorizontalAlignment="Center" Margin="0 -30 0 0">
                        <Ellipse>
                            <Ellipse.Fill>
                               <ImageBrush ImageSource="/Imagens/usu.png" />
                            </Ellipse.Fill>
                        </Ellipse>
                    </Border>

                    <TextBlock Text="Invest Safra" Foreground="black" FontSize="22" FontWeight="SemiBold"
                               DockPanel.Dock="Top" TextAlignment="Center" Margin="5 25 0 10"/>
                    <TextBlock Text="Lista de Área" Foreground="black" FontSize="14" FontWeight="SemiBold"
                               DockPanel.Dock="Top" TextAlignment="Center"/>

                    <Separator Margin="25" DockPanel.Dock="Top" Height="0.8" Background="black"/>

                    <StackPanel DockPanel.Dock="Top">
                        <Button Height="56" Background="#EFA82E" BorderBrush="#EFA82E" Click="Button_Click_3">
                            <DockPanel>
                                <Icon:PackIconMaterial Kind="HomeVariant" Style="{StaticResource ResourceKey=menuButtonIcon}" RenderTransformOrigin="0.133,0.48" Foreground="#FF020101"/>
                                <TextBlock Text="Menu" Width="49" Foreground="black"/>
                            </DockPanel>
                        </Button>
                    </StackPanel>

                    
                    
                </DockPanel>
            </Border>

            <Grid  Grid.Column="1" Margin="30 20 20 20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>


                


                <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Right">
                    <Button Style="{StaticResource topButton}"  Height="40"  x:Name="btSair" Width="48" Click="btSair_Click">
                        <Button.Background>
                            <ImageBrush ImageSource="/Icons/logout.png"/>
                        </Button.Background>
                    </Button>
                </StackPanel>

                <TextBlock Text="Área" Grid.Row="1" FontSize="32" FontWeight="SemiBold" Margin="0 20 0 0"
                           HorizontalAlignment="Left" Foreground="black"/>

                



                <Separator Height="1" Grid.Row="3" Background="white" Margin="0 -1 0 10"/>

                <!--<StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0 10 0 0">
                    <Button Content="Club Staff" Style="{StaticResource tabButton}"/>
                    <Button Content="Players" Style="{StaticResource tabButton}"/>
                    <Button Content="Archived" Style="{StaticResource tabButton}"/>
                </StackPanel>-->


                <TextBlock Text="Atualize ou Delete  Sua Área Aqui"  Grid.Row="2" FontSize="18"  FontWeight="SemiBold"
                           Foreground="Black" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="523,2,0,0" Width="330" Grid.RowSpan="2"/>



                <Grid Width="200" HorizontalAlignment="Right" Grid.Row="4">
                    <TextBlock Margin="65,12,31,0" VerticalAlignment="Top" Foreground="black" IsHitTestVisible="False"
                               Panel.ZIndex="1" Text="Procurar por Área..." Visibility="Visible"/>

                    <TextBox x:Name="txtFilter" Style="{StaticResource textBoxFilter}"/>

                    <Icon:PackIconMaterial Kind="Magnify" Height="15" VerticalAlignment="Center" Margin="0 0 15 0"
                                           Foreground="black" HorizontalAlignment="Right"/>
                </Grid>

                <DataGrid ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" RowStyle="{DynamicResource DataGridRowStyle1}" Grid.Row="5" x:Name="dataGridArea" IsReadOnly="true" AlternatingRowBackground="#FFE5E5E5" AlternationCount="2" AutoGenerateColumns="False" Background="#FFFAFAFA">
                    <DataGrid.Columns>

                        <!-- Começo CheckBox -->
                        <DataGridCheckBoxColumn CanUserResize="False" Width="auto">
                            <DataGridCheckBoxColumn.HeaderTemplate>
                                <DataTemplate>
                                    <CheckBox/>
                                </DataTemplate>
                            </DataGridCheckBoxColumn.HeaderTemplate>
                        </DataGridCheckBoxColumn>
                        <!-- Fim CheckBox -->
                        <!-- Começo -->
                        <DataGridTextColumn Header="Nome do Terreno" Binding="{Binding Nome_Terreno }"/>
                        <DataGridTextColumn Header="Responsável" Binding="{Binding Nome_Responsavel }"/>
                        <DataGridTextColumn Header="Metros" Binding="{Binding Metros}"/>
                        <DataGridTextColumn Header="CNPJ" Binding="{Binding CNPJ}"/>
                        <DataGridTextColumn Header="Localizção" Binding="{Binding Localizacao}"/>
                        <DataGridTextColumn Header="Descrição" Binding="{Binding Descricao}"/>

                        <DataGridTemplateColumn Header="Operation" IsReadOnly="True" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Click="btAtualizar_Click" Style="{StaticResource GridEditButton}">
                                            <Icon:PackIconMaterial Kind="PencilOutline" Style="{StaticResource GridButtonIcon}"/>
                                        </Button>
                                        <Button Click="btRemover_Click" Margin="20 0 0 0" Style="{StaticResource GridRemoveButton}">
                                            <Icon:PackIconMaterial Kind="DeleteOutline" Style="{StaticResource GridButtonIcon}"/>
                                        </Button>

                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <!-- Fim CheckBox -->


            </Grid>
        </Grid>
    </Border>

</Window>

