<Window x:Class="InvestSafra.Views.ListaInsumos"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Height="720" Width="1080" WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent">

    <Border Background="White" CornerRadius="30" MouseDown="Border_MouseDown" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="100"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
            </Grid>

            <Button Margin="0,37,0,653" Style="{StaticResource topButton}" Width="90">
                <Icon:PackIconMaterial Kind="Menu" Width="15" Height="15"/>
            </Button>

            <Border Background="#623ed0" CornerRadius="0 50 0 30" Margin="0,117,0,0">
                <DockPanel>
                    <Border Width="80" Height="80" Padding="3" Background="white" CornerRadius="100"
                            DockPanel.Dock="Top" HorizontalAlignment="Center" Margin="0 -30 0 0">
                        <Ellipse>
                            <Ellipse.Fill>
                                <ImageBrush/>
                            </Ellipse.Fill>
                        </Ellipse>
                    </Border>

                    <TextBlock Text="Silver Club" Foreground="white" FontSize="22" FontWeight="SemiBold"
                               DockPanel.Dock="Top" TextAlignment="Center" Margin="5 25 0 10"/>
                    <TextBlock Text="Menbers, France" Foreground="#d0bfff" FontSize="14" FontWeight="SemiBold"
                               DockPanel.Dock="Top" TextAlignment="Center"/>

                    <Separator Margin="25" DockPanel.Dock="Top" Height="0.8" Background="#7e5fdc"/>

                    <StackPanel DockPanel.Dock="Top">
                        <Button Height="56">
                            <DockPanel>
                                <Icon:PackIconMaterial Kind="HomeVariant" Style="{StaticResource ResourceKey=menuButtonIcon}" RenderTransformOrigin="0.133,0.48"/>
                                <TextBlock Text="Dashbord" Width="49"/>
                            </DockPanel>
                        </Button>
                    </StackPanel>

                    <StackPanel DockPanel.Dock="Top">
                        <Button Height="56">
                            <DockPanel>
                                <Icon:PackIconMaterial Kind="ForumOutline" Style="{StaticResource ResourceKey=menuButtonIcon}" RenderTransformOrigin="0.133,0.48"/>
                                <TextBlock Text="Dashbord" Width="49"/>
                            </DockPanel>
                        </Button>
                    </StackPanel>

                    <StackPanel DockPanel.Dock="Top">
                        <Button Style="{StaticResource menuButton}" VerticalAlignment="Bottom" DockPanel.Dock="Bottom">
                            <DockPanel>
                                <Icon:PackIconMaterial Kind="Logout" Style="{StaticResource ResourceKey=menuButtonIcon}" RenderTransformOrigin="0.133,0.48"/>
                                <TextBlock Text="Logout" Width="49"/>
                            </DockPanel>
                        </Button>
                    </StackPanel>

                </DockPanel>
            </Border>

            <Grid Grid.Column="1" Margin="30 20 20 20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>


                <Grid Width="300" HorizontalAlignment="Left">
                    <TextBlock Margin="20 0 " VerticalAlignment="Center" Foreground="#b0b9c6" IsHitTestVisible="False"
                               Panel.ZIndex="1" Text="Search hare..." Visibility="Visible"/>

                    <TextBox x:Name="txtSearch" Style="{StaticResource textBoxsearch}"/>
                    <Icon:PackIconMaterial Kind="Magnify" Width="15" Height="15" VerticalAlignment="Center"
                                           Margin="0 0 15 8" Foreground="white" HorizontalAlignment="Right"/>
                    
               
                
                </Grid>


                <Button Style="{StaticResource topButton}" Margin="0 0 20 0" Background="white"
                        HorizontalAlignment="Right">
                    <Icon:PackIconMaterial Kind="Bell"/>
                </Button>

                <TextBlock Text="Members" Grid.Row="1" FontSize="32" FontWeight="SemiBold" Margin="0 20 0 0"
                           HorizontalAlignment="Left" Foreground="black"/>

                <Button Style="{StaticResource addButton}">
                    <StackPanel Orientation="Horizontal">
                        <Icon:PackIconMaterial Kind="Plus" VerticalAlignment="Center" Width="11" Height="11" Margin="0 20 0 0"/>
                        <TextBlock Text="Add New Member" Height="19"/>
                    </StackPanel>
                </Button>



                <Separator Height="1" Grid.Row="3" Background="white" Margin="0 -1 0 10"/>

                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0 10 0 0">
                    <Button Content="Club Staff" Style="{StaticResource tabButton}"/>
                    <Button Content="Players" Style="{StaticResource tabButton}"/>
                    <Button Content="Archived" Style="{StaticResource tabButton}"/>
                </StackPanel>


                <TextBlock Text="68 Club Staff"  Grid.Row="4" FontSize="18"  FontWeight="SemiBold"
                           Foreground="Black" HorizontalAlignment="Right" VerticalAlignment="Center"/>

                <TextBlock Text="Filter by"  Grid.Row="4" FontSize="18"  FontWeight="SemiBold"
                           Foreground="Black" HorizontalAlignment="Right" VerticalAlignment="Center"/>



                <Grid Width="200" HorizontalAlignment="Right" Grid.Row="4">
                    <TextBlock Margin="15 0 " VerticalAlignment="Center" Foreground="#b0b9c6" IsHitTestVisible="False"
                               Panel.ZIndex="1" Text="Search is Members..." Visibility="Visible"/>

                    <TextBox x:Name="txtSearc2h" Style="{StaticResource textBoxsearch}"/>



                </Grid>

            </Grid>
        </Grid>
    </Border>

</Window>
