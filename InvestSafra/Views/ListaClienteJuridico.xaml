<Window x:Class="InvestSafra.Views.ListaClienteJuridico"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Height="720" Width="1080" WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent">

    <Border Background="White" CornerRadius="30" MouseDown="Border_MouseDown" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="100"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
            </Grid>

            <Border Background="#EFA82E" CornerRadius="0 50 0 30" Margin="0,117,0,0">
                <DockPanel>
                    <Border Width="80" Height="80" Padding="3" Background="white" CornerRadius="100"
                            DockPanel.Dock="Top" HorizontalAlignment="Center" Margin="0 -30 0 0">
                        <Ellipse>
                            <Ellipse.Fill>
                                <ImageBrush/>
                            </Ellipse.Fill>
                        </Ellipse>
                    </Border>

                    <TextBlock Text="Invest Safra" Foreground="white" FontSize="22" FontWeight="SemiBold"
                               DockPanel.Dock="Top" TextAlignment="Center" Margin="5 25 0 10"/>
                    <TextBlock Text="Lista de Cliente" Foreground="black" FontSize="14" FontWeight="SemiBold"
                               DockPanel.Dock="Top" TextAlignment="Center"/>

                    <Separator Margin="25" DockPanel.Dock="Top" Height="0.8" Background="black"/>

                    <StackPanel DockPanel.Dock="Top">
                        <Button Height="56" Background="#EFA82E" BorderBrush="#EFA82E" Click="Button_Click_3">
                            <DockPanel>
                                <Icon:PackIconMaterial Kind="HomeVariant" Style="{StaticResource ResourceKey=menuButtonIcon}" RenderTransformOrigin="0.133,0.48" Foreground="#FF020101"/>
                                <TextBlock Text="Menu" Width="49" Foreground="black"/>
                            </DockPanel>
                        </Button>
                    </StackPanel>
                    
                    <DockPanel/>

                </DockPanel>
            </Border>

            <Grid Grid.Column="1" Margin="30 20 20 20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>


                <Grid Width="300" HorizontalAlignment="Left"/>


                <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Right">
                    <Button Style="{StaticResource topButton}"  Height="40"  x:Name="btSair" Width="48" Click="btSair_Click">
                        <Button.Background>
                            <ImageBrush ImageSource="/Icons/logout.png"/>
                        </Button.Background>
                    </Button>
                </StackPanel>

                <TextBlock Text="Cliente" Grid.Row="1" FontSize="32" FontWeight="SemiBold" Margin="0 20 0 0"
                           HorizontalAlignment="Left" Foreground="black"/>

                <Button Style="{StaticResource addButton}">
                    <StackPanel Orientation="Horizontal">
                        <Icon:PackIconMaterial Kind="Plus" VerticalAlignment="Center" Width="11" Height="11" Margin="0 20 0 0"/>
                        <TextBlock Text="Add New Cliente Jurídico" Height="19"/>
                    </StackPanel>
                </Button>



                <Separator Height="1" Grid.Row="3" Background="white" Margin="0 -1 0 10"/>

                <!--<StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0 10 0 0">
                    <Button Content="Club Staff" Style="{StaticResource tabButton}"/>
                    <Button Content="Players" Style="{StaticResource tabButton}"/>
                    <Button Content="Archived" Style="{StaticResource tabButton}"/>
                </StackPanel>-->


                <TextBlock Text="Atualize ou Delete o Seu Cliente Jurídico Aqui"  Grid.Row="1" FontSize="18"  FontWeight="SemiBold"
                           Foreground="Black" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="424,60,0,0" Width="381" Grid.RowSpan="3"/>

                <DataGrid  ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" RowStyle="{DynamicResource DataGridRowStyle1}" Grid.Row="5" x:Name="dataGridClienteJuridico">
                    <DataGrid.Columns>
                        <!-- Começo CheckBox -->
                        <DataGridCheckBoxColumn CanUserResize="False" Width="auto">
                            <DataGridCheckBoxColumn.HeaderTemplate>
                                <DataTemplate>
                                    <CheckBox/>
                                </DataTemplate>
                            </DataGridCheckBoxColumn.HeaderTemplate>
                        </DataGridCheckBoxColumn>
                        <!-- Fim CheckBox -->
                        <!-- Começo -->
                       
                        <!-- Fim -->



                        <DataGridTextColumn Header="Nome" Binding="{Binding Nome}"/>
                        <DataGridTextColumn Header="Cpf" Binding="{Binding Cpf }"/>
                        <DataGridTextColumn Header="Sexo" Binding="{Binding Sexo}"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Descricao}"/>
                        <DataGridTextColumn Header="RG" Binding="{Binding Rg}"/>
                        <DataGridTextColumn Header="Telefone" Binding="{Binding Telefone}"/>
                        <DataGridTextColumn Header="Cidade" Binding="{Binding Cidade}"/>
                        <DataGridTextColumn Header="Estado" Binding="{Binding Estado}"/>
                        <DataGridTextColumn Header="CEP" Binding="{Binding Cep}"/>
                        <DataGridTextColumn Header="Rua" Binding="{Binding Rua}"/>
                        <DataGridTextColumn Header="Bairro" Binding="{Binding Bairro}"/>
                        <DataGridTextColumn Header="Complemento" Binding="{Binding Complemento}"/>

                        <DataGridTemplateColumn Header="Operation" IsReadOnly="True" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Style="{StaticResource GridEditButton}">
                                            <Icon:PackIconMaterial Kind="PencilOutline" Style="{StaticResource GridButtonIcon}"/>
                                        </Button>
                                        <Button Style="{StaticResource GridRemoveButton}">
                                            <Icon:PackIconMaterial Kind="DeleteOutline" Style="{StaticResource GridButtonIcon}"/>
                                        </Button>

                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>
    </Border>

</Window>

